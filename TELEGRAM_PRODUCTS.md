# ماژول خدمات مجازی

این ماژول به‌صورت مستقل به ربات اضافه شده و خرید، تمدید و تحویل سرویس‌های VPN را تغییر نمی‌دهد. پرداخت سفارش‌ها از همان ستون `user.Balance` انجام می‌شود.

## منوی اصلی

دکمه `خدمات مجازی` با شناسه `text_virtual_services` یکی از دکمه‌های واقعی کیبورد اصلی است. در صفحه چیدمان کیبورد پنل وب می‌توان آن را جابه‌جا یا حذف کرد و رنگ و ایموجی دکمه را مانند سایر دکمه‌ها تغییر داد.

نام دکمه از بخش `مدیریت خدمات مجازی > متن‌ها و تنظیمات` در پنل مدیریت ربات قابل تغییر است. مهاجرت دکمه برای دیتابیس‌های قدیمی فقط یک بار اجرا می‌شود؛ پس از آن چینش انتخابی مدیر حفظ خواهد شد.

## پنل مدیریت ربات

در کیبورد پنل ادمین، دکمه `مدیریت خدمات مجازی` اضافه شده است. امکانات این بخش:

- فعال یا غیرفعال کردن کل فروشگاه خدمات مجازی
- ساخت، تغییر نام، فعال‌سازی و غیرفعال‌سازی دسته‌ها
- ساخت مرحله‌ای محصول با انتخاب نوع فروش خودکار یا دستی در اولین مرحله
- تعیین قیمت، توضیحات، دسته و سطح کاربران مجاز
- تعیین رنگ و ایموجی پریمیوم مستقل برای دکمه هر دسته و محصول
- تعیین سقف خرید هر محصول برای هر کاربر
- هشدار قابل تنظیم کاهش موجودی محصولات خودکار
- دریافت اطلاعات دلخواه از مشتری، مانند یوزرنیم تلگرام
- افزودن گروهی کد یا لینک برای تحویل خودکار
- مشاهده سفارش‌های منتظر تحویل و سفارش‌های اخیر
- تحویل سفارش دستی
- لغو سفارش دستی و بازپرداخت اتمی مبلغ به کیف پول
- مشاهده آمار فروش
- شخصی‌سازی تمام متن‌های اصلی با ایموجی معمولی و پریمیوم
- ساخت خودکار تاپیک‌های `خدمات مجازی` و `خطاهای خدمات مجازی` در گروه گزارش

## مدل تحویل

`auto`: هر خط موجودی یک واحد مستقل است. هنگام پرداخت، موجودی و کیف پول داخل یک تراکنش قفل می‌شوند تا یک کد به دو کاربر فروخته نشود یا موجودی کاربر دوبار کسر نشود.

`manual`: پس از پرداخت، سفارش در پنل ادمین قرار می‌گیرد. مدیر می‌تواند متن، کد یا لینک تحویل را ارسال کند یا سفارش را لغو و مبلغ را به کیف پول بازگرداند.

## راه‌اندازی

SQL جداگانه لازم نیست. با اولین ورود به بخش خدمات مجازی، جدول‌ها و ستون‌های موردنیاز به‌صورت خودکار ساخته یا بروزرسانی می‌شوند.

برای گزارش‌ها، گروه گزارش باید سوپرگروه انجمنی باشد و ربات دسترسی `Manage Topics` داشته باشد. از مسیر `متن‌ها و تنظیمات > ساخت یا بازسازی تاپیک‌های گزارش` می‌توان وضعیت تاپیک‌ها را تکمیل کرد.

برای فروش مستقیم استارز یا پریمیوم به یوزرنیم کاربر، API و مشخصات سرویس تأمین‌کننده لازم است. ساختار فعلی اطلاعات یوزرنیم و سفارش را دریافت می‌کند و تحویل دستی یا تحویل خودکار از موجودی کد/لینک را انجام می‌دهد.
