# ماژول محصولات تلگرامی

این ماژول مستقل از فروش VPN است. دکمه `محصولات تلگرامی` به منوی اصلی اضافه می‌شود و تمام جدول‌های جدید با پیشوند `telegram_product_` ساخته می‌شوند. خریدها از همان ستون `user.Balance` انجام می‌شوند.

## راه‌اندازی

نیازی به اجرای SQL جداگانه نیست. اولین بار که کاربر دکمه محصولات تلگرامی یا ادمین یکی از دستورات زیر را اجرا کند، جدول‌ها خودکار ساخته می‌شوند.

ابتدا در ربات و با حساب ادمین یک دسته بسازید:

```text
/tg_add_category تلگرام پریمیوم
```

سپس با شناسه دسته‌ای که ربات برمی‌گرداند محصول بسازید:

```text
/tg_add_product 1|پریمیوم سه ماهه|500000|manual|تحویل پریمیوم سه ماهه برای اکانت کاربر
```

محصول خودکار با موجودی کد یا لینک:

```text
/tg_add_product 1|گیفت پریمیوم|600000|auto|تحویل خودکار لینک گیفت
/tg_add_stock 2|https://t.me/gift/example-link
```

هر بار اجرای `tg_add_stock` یک واحد موجودی اضافه می‌کند. پس از خرید، آن کد یا لینک فقط به همان خریدار تحویل داده می‌شود.

## دستورات ادمین

```text
/tg_help
/tg_products
/tg_orders
/tg_deliver 15|متن، کد یا لینک تحویل
/tg_product_on 2
/tg_product_off 2
```

محصول `manual` پس از کسر موجودی در فهرست سفارش‌های منتظر تحویل قرار می‌گیرد و برای ادمین‌ها پیام ارسال می‌شود. محصول `auto` فقط در صورت وجود موجودی قابل خرید است و بلافاصله تحویل می‌شود.

## نکته درباره استارز و پریمیوم

تحویل مستقیم استارز یا پریمیوم به نام کاربری کاربر به API تأمین‌کننده نیاز دارد. نسخه فعلی برای تحویل خودکار کد و لینک و برای سفارش دستی آماده است. اتصال به تأمین‌کننده باید با مستندات و کلید API همان سرویس انجام شود؛ این بخش عمداً به فروش VPN متصل نشده است.
